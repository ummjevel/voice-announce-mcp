# voice-announce-mcp

- After finishing a task (or a significant chunk of work), call the
  `voice-announce` MCP server's `announce` tool with a very short summary
  (1-2 lines), written entirely in Korean, of what was done. Write the
  summary yourself; the tool only does text -> speech -> playback, no
  summarization or translation.
