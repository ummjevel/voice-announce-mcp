# voice-announce-mcp

- After finishing a task (or a significant chunk of work), call the
  `voice-announce` MCP server's `announce` tool with a 1-3 sentence summary
  of what was done. Write the summary yourself; the tool only does
  text -> speech -> playback, no summarization.
